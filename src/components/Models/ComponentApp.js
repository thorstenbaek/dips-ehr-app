class ComponentApp {
    constructor(component, label) {
        this.component = component;
        this.label = label;
    }

    getComponent()
    {
        return (this.component);
    }
}

export default ComponentApp;